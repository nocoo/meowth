{
  "name": "@meowth/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO: setup web dev server'",
    "build": "echo 'TODO: setup web build'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist .next"
  },
  "dependencies": {
    "@meowth/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
