import { test } from '@playwright/test';

// Phase 2.9 placeholder — dashboardEmbedFixture (08 §3.4.2).
// Real body lands when daemon embeds dashboard dist (Phase 3.6 / 02 §6)
// and 07 §11 L3 (CSP / XSS / Tokens modal) is implementable; until then
// the suite is honest-skipped.
test.skip('embed fixture suite placeholder — wires daemon embed (3.6) + 07 §11 L3 flows', () => {
  // intentionally empty
});
